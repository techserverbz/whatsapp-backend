node_modules/
dist/
tokens/
data/
.env
*.log
userDataDir/
.wwebjs_auth/
.wwebjs_cache/
# WhatsApp session storage (LocalAuth / WPPConnect tokens) — never commit.
# Leading slash anchors these to the repo root: unanchored `webjs/` also matched
# src/engines/webjs/, silently excluding the engine source from the repo.
/webjs/
/wwp/
